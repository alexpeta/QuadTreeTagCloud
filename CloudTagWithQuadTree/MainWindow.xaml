<Window x:Class="CloudTagWithQuadTree.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Summary}" SizeToContent="WidthAndHeight" ResizeMode="CanMinimize"
        x:Name="WindowContainer">

    <ItemsControl ItemsSource="{Binding Words}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas IsItemsHost="True" Background="LightGray" Width="{Binding CanvasWidth}" Height="{Binding CanvasHeight}" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Text}" FontFamily="Segoe UI" FontSize="{Binding FontSize}" Foreground="Black" Padding="0" Margin="0" ToolTip="{Binding}">
                    <TextBlock.LayoutTransform>
                        <RotateTransform Angle="{Binding Angle}" />
                    </TextBlock.LayoutTransform>
                </TextBlock>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.ItemContainerStyle>
            <Style>
                <Setter Property="Canvas.Left" Value="{Binding Path=Left}" />
                <Setter Property="Canvas.Top" Value="{Binding Path=Top}" />
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>
   
    
</Window>
